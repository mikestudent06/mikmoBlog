<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>About MikMo | <%= title %></title>
    <style>
      /* Reset some default styles */
      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }

      /* Apply some global styles */
      body {
        font-family: sans-serif;
        font-size: 16px;
        line-height: 1.5;
        color: #333;
      }

      /* Style the header and navigation */
      header {
        background-color: #eee;
        padding: 1rem;
      }

      .site-title {
        display: flex;
        align-items: center;
        font-size: 2rem;
      }

      /* Style the main content */
      main {
        max-width: 800px;
        margin: 0 auto;
        padding: 2rem;
      }

      .about-content {
        margin-bottom: 2rem;
      }

      .about-content h1 {
        font-size: 2.5rem;
        margin-bottom: 1rem;
      }

      .about-content p {
        margin-bottom: 1rem;
      }
    </style>
  </head>